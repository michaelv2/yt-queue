# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
yt-dlp==2026.8.19
yt-dlp-ejs==0.8.0
faster-whisper>=1.0.0
aiosqlite>=0.19.0

# CLI tools
requests>=2.31.0

# Optional LLM providers — install as needed:
#   pip install anthropic        # YTQUEUE_LLM_PROVIDER=anthropic
#   pip install openai           # YTQUEUE_LLM_PROVIDER=openai
# Ollama uses only stdlib urllib — no extra package required.
# anthropic>=0.40.0
# openai>=1.0.0
